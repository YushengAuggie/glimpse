#!/usr/bin/env bash
# Backstop before pushing. With gitleaks this scans full git history; without it,
# the fallback scans tracked files only (install gitleaks for history coverage).
exec "$(git rev-parse --show-toplevel)/scripts/check-secrets.sh" all
